// Copyright (c) Mysten Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

/// This module defines the Moves Pokemons can use in battle.
/// Moves are different types of attacks that can be used in battle, some moves
/// can be used to inflict physical damage, some can be used to inflict special
/// damage.
module heroes::moves {
    // use std::vector as v;
    // use heroes::stats::{Self, Stats};

    // Let's start with just two types of moves, physical and special.
    const PHYSICAL: u64 = 0;
    const SPECIAL: u64 = 1;
}
